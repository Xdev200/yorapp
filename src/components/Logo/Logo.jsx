import React from 'react'
import styles from '../../styles/logo.module.css'

const Logo = () => {
    return (
        <img src='/images/brand_icon.svg' alt='brand icon' className={styles.logo} />
    )
}

export default Logo
